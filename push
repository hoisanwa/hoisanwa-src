#!/bin/sh

obsidianhtml convert -i config.yaml

GIT=`which git`
REPO_DIR=../hoisanwa.github.io
cd ${REPO_DIR}

mv html/* ./
rm -rf html/

${GIT} add .
${GIT} commit -m "update"
${GIT} push -f origin main